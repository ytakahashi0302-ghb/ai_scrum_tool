# Epic 31: Git Worktree隔離 & 1-Clickレビュー (Handoff Document)

## プロジェクトの現在地
現在、MicroScrum AI の「複数エージェント並行稼働時のコード競合を防ぐためのGit Worktree完全隔離」および「1-ClickレビューUI」を実装する Epic 31 の真っ最中です。
前任のAIエージェントによって **Phase 1（バックエンド基盤）までが完全に実装・テスト完了** しています。
あなたは新しいAIエージェントとして、**Phase 2** から作業を引き継いでください。

## 前任者（Phase 1）の完了成果物
- `src-tauri/src/worktree.rs` を新設し、Tauriコマンド（5種）を実装済み。
- ワークツリーの動的生成、ブランチ作成、`node_modules` のシンボリックリンク共有、コンフリクト時の `--abort` 処理、自動クリーンアップロジックが完成。
- `cargo test` にて24項目の正常系/異常系テストが全てパスしている状態。

## あなたのミッション（Phase 2: DB拡張とReviewステータス）
Phase 1で完成した堅牢なバックエンドを、フロントエンドや既存のタスク管理システムと繋ぐための「状態管理（DB）」の拡張を行います。

1. **DBスキーマの拡張 (SQLite)**
   - `src-tauri/src/db.rs` (または該当のスキーマ定義ファイル) を修正し、タスクのステータスに `Review` を追加してください（既存は `Todo`, `InProgress`, `Done` 等）。
   - タスクと紐づくワークツリーのパスや状態を管理するためのカラム、または新しいテーブル（`worktrees`）を追加・マイグレーションしてください。
2. **ステータス遷移ロジックの修正**
   - エージェントが作業を完了した際、これまでは直接 `Done` にしていましたが、これを `Review` ステータスに遷移するようにバックエンドのロジックを修正してください。

## 注意事項
- Phase 1で構築された `worktree.rs` のロジックは極めて完成度が高いため、破壊的な変更は加えないでください。
- 実装を開始する前に、必ず `implementation_plan.md` と `task.md` の Phase 2 の項目を読み込んでから作業を開始してください。