# Epic 39: 設定画面 - セットアップ状況タブ タスクリスト

## ステータス

- 状態: `Done`
- 着手条件: Epic 36 完了（CLI 検出コマンドが利用可能であること）
- 作成日: 2026-04-09
- 完了日: 2026-04-09

## 概要

GlobalSettingsModal に「セットアップ状況」タブを新設し、環境の準備状態（Git、各 CLI、API キー）を一覧で確認できるようにする。未セットアップ項目がある場合はこのタブをデフォルト表示する。

## 実行順序

### 1. SetupStatusTab コンポーネントの作成
- [x] `src/components/ui/SetupStatusTab.tsx` を新規作成する。
- [x] 以下の情報をカード形式で表示する:
  - **環境ツール**
    - Git: インストール状態 + バージョン
    - Claude Code CLI: インストール状態 + バージョン
    - Gemini CLI: インストール状態 + バージョン
    - Codex CLI: インストール状態 + バージョン
  - **API キー**
    - Anthropic: 設定済み / 未設定
    - Gemini: 設定済み / 未設定
- [x] 各項目にステータスアイコン（チェック / 警告）を表示する。
- [x] 未インストールの CLI には簡潔なインストール手順リンクを添える。
- [x] 「再検出」ボタンを配置し、CLI 検出を手動でリフレッシュできるようにする。

### 2. API キー設定状態の検出
- [x] Tauri store (`settings.json`) から API キーの有無を取得するロジックを実装する。
- [x] キーの値自体は表示せず、「設定済み」「未設定」のみ表示する。

### 3. GlobalSettingsModal へのタブ追加
- [x] `src/components/ui/GlobalSettingsModal.tsx` のタブ構成を変更する:
  - 新タブ順: セットアップ状況 / POアシスタント設定 / チーム設定 / プロジェクト設定
- [x] 未セットアップ項目がある場合（CLI が1つもインストールされていない、または API キーが未設定）、セットアップ状況タブをデフォルトで開くロジックを追加する。

### 4. 最低要件の明示
- [x] タブ内に以下のガイダンスを表示する:
  - 「Dev エージェント機能には、最低1つの CLI ツール + Git が必要です」
  - 「PO アシスタント機能には、API キー設定 または CLI ツールが必要です」（Phase 3 以降で更新）

### 5. 動作確認
- [x] 各ツールのインストール有無が正しく表示されること。
- [x] API キーの設定有無が正しく表示されること。
- [x] 再検出ボタンで CLI 検出結果が更新されること。
- [x] 未セットアップ時にタブがデフォルトで選択されること。

## 補足対応

- [x] Windows の npm グローバル CLI (`*.cmd` shim) を検出できるように、CLI 検出ロジックを補強した。
- [x] セットアップ状況タブの再検出導線を改善し、ボタン文言と更新完了トーストを追加した。

## 追加対応: Team Settings CLI 対応

- [x] チーム設定タブで、ロールごとに `Claude / Gemini / Codex` の CLI 種別を選択できるようにする。
- [x] CLI 種別ごとに、モデル入力欄と補助ガイダンスを切り替える。
- [x] Codex CLI の導入リンクを `https://developers.openai.com/codex/cli` に更新する。

## 追加対応: テンプレート初期値と導入制御

- [x] 未導入 CLI はチーム設定タブで選択できないようにする。
- [x] 旧 1 テンプレート構成または空構成を、推奨 5 テンプレート構成へ自動補完する。

## 追加対応: Analytics 分離とテンプレート強制補完

- [x] LLM Observability をプロジェクト設定から分離し、アナリティクスタブへ移す。
- [x] 既存テンプレートが 1 件だけの場合は、その 1 件をリードエンジニア枠として扱い、残り 4 ロールを強制追加する。
- [x] 新規プロジェクト作成時に、チームテンプレートが常に 5 件そろうように補完する。

## 追加対応: Gemini 実行エラーとエラー表示先の修正

- [x] Gemini CLI 検出時に解決した実体パスを、実行時にも再利用して起動失敗を防ぐ。
- [x] 起動失敗時のエラーイベントにタスク情報を含め、該当タスクのターミナルタブへ表示する。
- [x] Gemini ロール実行時にも、起動トーストやエラーメッセージが CLI 種別に沿った文言になるよう整える。
- [x] Gemini CLI の現行 headless 引数仕様に合わせ、位置引数衝突を起こすオプションを除去する。
- [x] Gemini が読めるワークスペース内に prompt ファイルを配置し、ワークスペース外ファイル参照エラーを防ぐ。

## 追加対応: Analytics 表示のノイズ削減と CLI 表記補正

- [x] 0ドルの Source / Model 内訳を表示しないようにする。
- [x] CLI usage の provider 表記を Claude 固定にせず、Gemini / Codex / Claude を正しく区別する。
- [x] CLI usage の注意文を Claude 固定ではなく汎用的な案内へ更新する。

## 追加対応: Dev エージェント既定アバター割り当て

- [x] `public/avatars` の新しいファイル名に合わせて generic fallback の参照先を更新する。
- [x] テンプレート 1〜5 に対して、`dev-agent-1.png` から `dev-agent-5.png` を番号順に既定割り当てする。
- [x] 既存テンプレートでも `avatar_image` が未設定なら、テンプレート順に既定アバターを補完する。
